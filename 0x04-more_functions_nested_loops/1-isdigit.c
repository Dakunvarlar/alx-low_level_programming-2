#include "main.h"
/**
 * _isadigit - checks whether character is a digit
 * @c: is to be used as argument
 * Return: 1 on success and 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
