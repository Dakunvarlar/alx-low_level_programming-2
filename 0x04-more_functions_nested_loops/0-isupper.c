#include "main.h"

/**
 * _is alpha - function checks if c iuppercase
 * Return: 1 if c is uppercase
 * Return: 0 on error
 */
int _isupper(int c);
{
	if (c >= 'A' && c <= 'Z' )
	{
		return (1);
	}
	else
	return (0);
}
