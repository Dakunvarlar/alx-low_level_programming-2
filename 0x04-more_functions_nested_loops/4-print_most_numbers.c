#include "main.h"

/**
 * void print_most_numbers(void) - prints digits 0-9 without 2 and 4
 * Return: Nothing
 */

void print_most_numbers(void);
{
	int c;

	for (c = 0; c <= 0; c++)
	{
		while (c != 2 ||  c != 4)
			_putchar(c);
	}
	_putchar('\n');

}
