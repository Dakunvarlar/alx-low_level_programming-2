#include "main.h"
/**
 * _abs - chaecks the code for Holberton School students
 * @n: is an integer argument
 * Return: Always 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
